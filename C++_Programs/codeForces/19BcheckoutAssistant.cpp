#include <bits/stdc++.h>
using namespace std;
#define ll long long

// bool myCompare(pair<int,int> p1, pair<int,int> p2){
// 	return p1.second < p2.second;
// }

void solver(vector<int> time, vector<int> cost, int & sol, int index, int curr){
	time()
}

int main() {
	int n;
	cin>>n;
	ll cost = 0;
	vector<int> time,cost;
	for(int i = 0 ; i < n ; i++){
		int t,c;
		cin>>t>>c;
		time.push_back(t);
		cost.push_back(c);
	}
	int sol = 0;
	solver(time,cost,sol,0,0);
}