#include <bits/stdc++.h>
using namespace std;

int largestRectangle(vector<int> & height) {
	int ret = 0;
	height.push_back(0);
	vector<int> index;
	for (int i=0; i<height.size(); i++) {
		
	}
}

int main() {

	return 0;
}