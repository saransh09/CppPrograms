#include <bits/stdc++.h>
using namespace std;

int minJums(vector<int> arr, int l, int h){
	if(h==l) return 0;
	if(arr[l]==0) return INT_MAX;
	int min = INT_MAX;
	for(int i = l+1 ; i <= h ; i++) {
		
	}
}

int main() {

}