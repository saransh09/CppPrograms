#include <bits/stdc++.h>
using namespace std;

int count(vector<int> coins, int sum){
	int table[coins.size()+1][sum];
	for(int i = 0 ; i < sum ; i++){
		table[0][i]=1;
	}
	for(int i = 1 ; i < coins.size() + 1 ; i++){
		for(int j = 0 ; j < sum ; j++) {
			x = (i - coins[])
		}
	}
}

int main() {

	return 0;
}