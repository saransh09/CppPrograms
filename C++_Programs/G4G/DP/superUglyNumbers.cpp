#include <bits/stdc++.h>
using namespace std;

int superUgly(int n, vector<int> primes){
	vector<int> nextMultiples(primes.size());
	int multiples_of[k];
	memset(multiples_of,0,sizeof(multiples_of));
	set<int> ugly;
	ugly.insert(1);
	while(ugly.size()!=0){
		
	}
}

int main() {

	return 0;
}