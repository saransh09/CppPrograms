#include <bits/stdc++.h>
using namespace std;

class Solution{
	maxSumISHelper(vector<int> arr, int n){
		int dp[n], maxSum = 0;
		for(int i = 0 ; i < n ; i++) dp[i] = arr[i];
		for(int i = 0 ; i < n ; i++){
			
		}
	}
public:
	int maxSumIS(vector<int> arr){
		return maxSumISHelper(arr,arr.size());
	}
};

int main() {

	return 0;
}